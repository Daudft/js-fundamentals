Scripting language — what it is and how it works
var, let, const — differences, scope (function vs block), update/re-declare rules
alert() and prompt() — showing output and taking input
Type coercion issue — prompt() always returns a string, so numbers must be converted using Number() before adding with +
Practice — creating variables, using prompt/alert, and guess-the-output exercises (string vs number concatenation, var redeclaring, block scoping)